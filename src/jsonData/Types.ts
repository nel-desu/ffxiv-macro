export type JMacro = {
  cmd_zh: string,
  cmd_en: string,
  cmd_zh_abbr: string,
  cmd_en_abbr: string,
  desc: string,
  type: string,
}

export type JTarget = {
  cmd_zh: string,
  cmd_en: string,
  desc: string,
}